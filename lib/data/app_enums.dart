/* type represents if its a base selection or converting selection
   * pass base when it is a base selection
   * pass converting when it is a converting selection
   */
enum CurrencySelectionType {
  base,
  converting,
}